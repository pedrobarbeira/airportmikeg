//Created by Pedro Barbeira (up201303693;
#include "voyage.h"

bool Voyage::addConnection(Airport* o, Airport* c, Airport* d){
    std::list<Airport*>::iterator it;
    /**If routes.front()->getOrigin() == d add conection to front;
     * Else search list till it->getDestination == o and it->getOrigin == d
     *      create connection and insert it
     *      return true
     * it not found return false
     */
}
